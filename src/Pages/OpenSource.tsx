import React from "react";
import Header from "../Components/Header";

const OpenSource = () => {
  return (
    <div>
      <Header />
      open source
    </div>
  );
};

export default OpenSource;
